source "https://rubygems.org"

# This single gem mirrors exactly what GitHub Pages runs on its servers,
# so a local build behaves identically to production.
gem "github-pages", group: :jekyll_plugins

# Faster local rebuilds on macOS/Windows
gem "webrick", "~> 1.8"
